#!/usr/bin/env bash
# Placeholder — implementation pending in a later milestone.
# Will verify the commenter has the required role (maintainer/author/anyone)
# via GET /repos/{owner}/{repo}/collaborators/{username}/permission.
set -euo pipefail

printf '{"level":"info","script":"%s","event":"placeholder_invoked","outcome":"noop"}\n' "$(basename "$0")" >&2

exit 0
