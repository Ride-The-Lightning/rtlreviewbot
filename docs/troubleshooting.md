# Troubleshooting

Placeholder. Implementation pending.

This document will collect common operational issues and their fixes:

- Authentication failures (bad App ID, bad PEM, key rotated)
- Permission errors (consumer repo not granted access to org secrets)
- Workflow not triggering (missing shim, wrong pinned tag)
- Bot not posting reviews (rate-limit, diff too large, skill failure)
